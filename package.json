{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build --dotenv .env.production --log-level silent",
    "build:dev": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate --dotenv .env.production --log-level silent",
    "generate:dev": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "1.1.5",
    "@nuxt/image": "1.5.0",
    "@nuxt/ui": "2.13.0",
    "cross-env": "10.0.0",
    "nuxt": "3.10.1"
  },
  "dependencies": {
    "@vueuse/motion": "2.1.0"
  }
}
